import type { ReactNode } from "react";

import PixAlbumIcon from "./PixAlbumIcon";

export const gameIcons: Record<string, ReactNode> = {
    pixalbum: <PixAlbumIcon />,

    // Add other games here later:
    // quaver: <QuaverIcon />,
    // wordle: <WordleIcon />,
};